<form class="form-delete" method="post" action="{{ path('equipage_delete', {'id': equipage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipage.id) }}">
    <button class="btn p-0"><i class="far fa-trash-alt text-danger"></i></button>
</form>
